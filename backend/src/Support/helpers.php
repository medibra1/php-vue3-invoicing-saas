<?php

declare(strict_types=1);

// Global helper functions, autoloaded by Composer ("files" in composer.json).
// Intentionally empty for now — populated as cross-cutting helpers
// (e.g. env(), config()) become necessary.
